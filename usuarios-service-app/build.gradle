apply plugin: 'org.springframework.boot'
apply plugin:  'application'
mainClassName = 'br.com.usuarios.Applicatiom'

dependencies {
    compile project(":usuarios-service-contract")
    compile(dimedServicesDependencies.app)
}
